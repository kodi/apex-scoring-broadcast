/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
    return knex.raw(`
       ALTER TABLE game ADD COLUMN "livedata" int4;
       UPDATE game SET livedata = (SELECT id FROM LIVEDATA WHERE "gameId" = game.id);
    `);
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
  
};
